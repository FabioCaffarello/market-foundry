# Alert rules for market-foundry SLOs and runtime invariants.
#
# Filled by H-5 commit 7. This commit (H-5 commit 5) creates the
# file with an empty `groups` list so Prometheus boots without
# rule_files complaints.

groups: []
