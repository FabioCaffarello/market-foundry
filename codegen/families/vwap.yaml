family:
  name: vwap
  layer: signal
  tier: 1

nats:
  subject: "signal.events.vwap.generated.>"
  event_type: "signal.events.v1.vwap_generated"
  stream: SIGNAL_EVENTS
  durable: writer-signal-vwap

writer:
  table: signals
  columns: "event_id, occurred_at, correlation_id, causation_id, type, source, symbol, timeframe, value, metadata, final, timestamp"
  mapper: mapSignalRow
  pipeline_family_key: vwap
  config_array: signal_families

domain:
  event_package: signal
  event_type: SignalGeneratedEvent
