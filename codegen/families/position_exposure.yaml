family:
  name: position_exposure
  layer: risk
  tier: 1

nats:
  subject: "risk.events.position_exposure.assessed.>"
  event_type: "risk.events.v1.position_exposure_assessed"
  stream: RISK_EVENTS
  durable: writer-risk-position-exposure

writer:
  table: risk_assessments
  columns: "event_id, occurred_at, correlation_id, causation_id, type, source, symbol, base, quote, contract, timeframe, disposition, confidence, strategies, constraints, rationale, parameters, metadata, final, timestamp"
  mapper: mapRiskRow
  pipeline_family_key: position_exposure
  config_array: risk_families

domain:
  event_package: risk
  event_type: RiskAssessedEvent
