family:
  name: candle
  layer: evidence
  tier: 1

nats:
  subject: "evidence.events.candle.sampled.>"
  event_type: "evidence.events.v1.candle_sampled"
  stream: EVIDENCE_EVENTS
  durable: writer-candle

writer:
  table: evidence_candles
  columns: "event_id, occurred_at, correlation_id, causation_id, source, symbol, base, quote, contract, timeframe, open, high, low, close, volume, trade_count, open_time, close_time, final"
  mapper: mapCandleRow
  pipeline_family_key: candle
  config_array: families

domain:
  event_package: evidence
  event_type: CandleSampledEvent
